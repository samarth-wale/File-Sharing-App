// jsconfig.json

{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/components/UploadForm": ["(dashboard)/(routes)/upload/_components/UploadForm"],
      "@/components/ProgressBar": ["(dashboard)/(routes)/upload/_components/progressBar"],
      "@/firebaseConfig": ["firebaseConfig"]
    }
  }
}
